/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "eventdeffuncclient.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<EventdeffuncclientTable> registry;

	Eventdeffuncclient::Eventdeffuncclient(StreamReader& reader)
	{
		index = reader.ReadInt32();
		classId = reader.ReadInt32();
		trigger = reader.ReadInt32();
		deffuncId = reader.ReadInt32();
	}

	void EventdeffuncclientTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "EVENTDEFFUNCCLIENT.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const Eventdeffuncclient& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto EventdeffuncclientTable::Find(int32_t index) const -> const Eventdeffuncclient*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto EventdeffuncclientTable::Get() const -> const std::vector<Eventdeffuncclient>&
	{
		return _vector;
	}
}
