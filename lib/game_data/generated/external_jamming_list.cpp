/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "external_jamming_list.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ExternalJammingListTable> registry;

	ExternalJammingList::ExternalJammingList(StreamReader& reader)
	{
		index = reader.ReadInt32();
		jammingType = reader.ReadInt32();
		className = reader.ReadString(reader.ReadUInt16());
		windowName = reader.ReadString(reader.ReadUInt16());
		fileChecksum = reader.ReadInt32();
		fileSize = reader.ReadInt32();
		numSpare1 = reader.ReadInt32();
		internalName = reader.ReadString(reader.ReadUInt16());
		legalCopyright = reader.ReadString(reader.ReadUInt16());
		originalFilename = reader.ReadString(reader.ReadUInt16());
		productionVersion = reader.ReadString(reader.ReadUInt16());
		comments = reader.ReadString(reader.ReadUInt16());
		privateBuild = reader.ReadString(reader.ReadUInt16());
		specialBuild = reader.ReadString(reader.ReadUInt16());
		spareField = reader.ReadString(reader.ReadUInt16());
	}

	void ExternalJammingListTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "EXTERNAL_JAMMING_LIST.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const ExternalJammingList& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ExternalJammingListTable::Find(int32_t index) const -> const ExternalJammingList*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ExternalJammingListTable::Get() const -> const std::vector<ExternalJammingList>&
	{
		return _vector;
	}
}
