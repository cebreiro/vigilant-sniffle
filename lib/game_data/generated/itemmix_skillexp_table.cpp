/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "itemmix_skillexp_table.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ItemmixSkillexpTableTable> registry;

	ItemmixSkillexpTable::ItemmixSkillexpTable(StreamReader& reader)
	{
		index = reader.ReadInt32();
		difficultyA = reader.ReadInt32();
		difficultyB = reader.ReadInt32();
		difficultyC = reader.ReadInt32();
	}

	void ItemmixSkillexpTableTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "ITEMMIX_SKILLEXP_TABLE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const ItemmixSkillexpTable& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ItemmixSkillexpTableTable::Find(int32_t index) const -> const ItemmixSkillexpTable*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ItemmixSkillexpTableTable::Get() const -> const std::vector<ItemmixSkillexpTable>&
	{
		return _vector;
	}
}
