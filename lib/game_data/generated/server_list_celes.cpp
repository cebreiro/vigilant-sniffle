/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "server_list_celes.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ServerListCelesTable> registry;

	ServerListCeles::ServerListCeles(StreamReader& reader)
	{
		index = reader.ReadInt32();
		name = reader.ReadString(reader.ReadUInt16());
		desc = reader.ReadString(reader.ReadUInt16());
		ip = reader.ReadString(reader.ReadUInt16());
	}

	void ServerListCelesTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "SERVER_LIST_CELES.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const ServerListCeles& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ServerListCelesTable::Find(int32_t index) const -> const ServerListCeles*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ServerListCelesTable::Get() const -> const std::vector<ServerListCeles>&
	{
		return _vector;
	}
}
