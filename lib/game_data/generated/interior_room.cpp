/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "interior_room.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<InteriorRoomTable> registry;

	InteriorRoom::InteriorRoom(StreamReader& reader)
	{
		index = reader.ReadInt32();
		roomId = reader.ReadInt32();
		roomType = reader.ReadInt32();
		roomLv = reader.ReadInt32();
		roomCost = reader.ReadInt32();
		roomDesc = reader.ReadInt32();
		roomPreview = reader.ReadInt32();
	}

	void InteriorRoomTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "INTERIOR_ROOM.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const InteriorRoom& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto InteriorRoomTable::Find(int32_t index) const -> const InteriorRoom*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto InteriorRoomTable::Get() const -> const std::vector<InteriorRoom>&
	{
		return _vector;
	}
}
