/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "string_table_korean.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<StringTableKoreanTable> registry;

	StringTableKorean::StringTableKorean(StreamReader& reader)
	{
		index = reader.ReadInt32();
		string = reader.ReadString(reader.ReadUInt16());
		desc = reader.ReadString(reader.ReadUInt16());
	}

	void StringTableKoreanTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "StringTableKorean.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const StringTableKorean& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto StringTableKoreanTable::Find(int32_t index) const -> const StringTableKorean*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto StringTableKoreanTable::Get() const -> const std::vector<StringTableKorean>&
	{
		return _vector;
	}
}
