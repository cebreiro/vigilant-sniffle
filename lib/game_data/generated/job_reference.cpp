/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "job_reference.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<JobReferenceTable> registry;

	JobReference::JobReference(StreamReader& reader)
	{
		index = reader.ReadInt32();
		maxJobLevel = reader.ReadInt32();
		commonJobMod = reader.ReadInt32();
		mHPFactor = reader.ReadFloat32();
		mSPFactor = reader.ReadFloat32();
		pHRatFactor = reader.ReadFloat32();
		defenseFactor = reader.ReadFloat32();
		hPRecoveryFactor = reader.ReadFloat32();
		sPRecoveryFactor = reader.ReadFloat32();
		avatarSpeedFactor = reader.ReadFloat32();
		comment = reader.ReadString(reader.ReadUInt16());
	}

	void JobReferenceTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "JOB_REFERENCE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const JobReference& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto JobReferenceTable::Find(int32_t index) const -> const JobReference*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto JobReferenceTable::Get() const -> const std::vector<JobReference>&
	{
		return _vector;
	}
}
