/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "monster_move_type.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<MonsterMoveTypeTable> registry;

	MonsterMoveType::MonsterMoveType(StreamReader& reader)
	{
		index = reader.ReadInt32();
		movename = reader.ReadString(reader.ReadUInt16());
		movespeed = reader.ReadFloat32();
		gyrationangle1 = reader.ReadInt32();
		gyrationangle2 = reader.ReadInt32();
	}

	void MonsterMoveTypeTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "MONSTER_MOVE_TYPE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const MonsterMoveType& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto MonsterMoveTypeTable::Find(int32_t index) const -> const MonsterMoveType*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto MonsterMoveTypeTable::Get() const -> const std::vector<MonsterMoveType>&
	{
		return _vector;
	}
}
