/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "string_subs.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<StringSubsTable> registry;

	StringSubs::StringSubs(StreamReader& reader)
	{
		index = reader.ReadInt32();
		org = reader.ReadString(reader.ReadUInt16());
		subs = reader.ReadString(reader.ReadUInt16());
	}

	void StringSubsTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "STRING_SUBS.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const StringSubs& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto StringSubsTable::Find(int32_t index) const -> const StringSubs*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto StringSubsTable::Get() const -> const std::vector<StringSubs>&
	{
		return _vector;
	}
}
