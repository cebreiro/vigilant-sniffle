/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "itemmix_judge_table.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ItemmixJudgeTableTable> registry;

	ItemmixJudgeTable::ItemmixJudgeTable(StreamReader& reader)
	{
		index = reader.ReadInt32();
		fail = reader.ReadInt32();
		gradeLow = reader.ReadInt32();
		gradeModdle = reader.ReadInt32();
		gradeHigh = reader.ReadInt32();
		gradSUper = reader.ReadInt32();
	}

	void ItemmixJudgeTableTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "ITEMMIX_JUDGE_TABLE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const ItemmixJudgeTable& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ItemmixJudgeTableTable::Find(int32_t index) const -> const ItemmixJudgeTable*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ItemmixJudgeTableTable::Get() const -> const std::vector<ItemmixJudgeTable>&
	{
		return _vector;
	}
}
