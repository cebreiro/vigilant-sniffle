/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "lv_db.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<LvDbTable> registry;

	LvDb::LvDb(StreamReader& reader)
	{
		index = reader.ReadInt32();
		job = reader.ReadInt32();
		jobStatus = reader.ReadInt32();
		power = reader.ReadInt32();
		speed = reader.ReadInt32();
		health = reader.ReadInt32();
		_int = reader.ReadInt32();
		will = reader.ReadInt32();
	}

	void LvDbTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "LV_DB.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const LvDb& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto LvDbTable::Find(int32_t index) const -> const LvDb*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto LvDbTable::Get() const -> const std::vector<LvDb>&
	{
		return _vector;
	}
}
