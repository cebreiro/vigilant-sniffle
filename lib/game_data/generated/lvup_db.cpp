/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "lvup_db.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<LvupDbTable> registry;

	LvupDb::LvupDb(StreamReader& reader)
	{
		index = reader.ReadInt32();
		lvupVg = reader.ReadInt32();
		lvupG = reader.ReadInt32();
		lvupL = reader.ReadInt32();
		lvupP = reader.ReadInt32();
	}

	void LvupDbTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "LVUP_DB.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const LvupDb& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto LvupDbTable::Find(int32_t index) const -> const LvupDb*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto LvupDbTable::Get() const -> const std::vector<LvupDb>&
	{
		return _vector;
	}
}
