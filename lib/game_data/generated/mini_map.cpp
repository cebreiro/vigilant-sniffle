/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "mini_map.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<MiniMapTable> registry;

	MiniMap::MiniMap(StreamReader& reader)
	{
		index = reader.ReadInt32();
		toolTip = reader.ReadString(reader.ReadUInt16());
		icon1 = reader.ReadInt32();
		icon2 = reader.ReadInt32();
	}

	void MiniMapTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "MINI_MAP.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const MiniMap& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto MiniMapTable::Find(int32_t index) const -> const MiniMap*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto MiniMapTable::Get() const -> const std::vector<MiniMap>&
	{
		return _vector;
	}
}
