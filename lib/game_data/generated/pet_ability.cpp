/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "pet_ability.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<PetAbilityTable> registry;

	PetAbility::PetAbility(StreamReader& reader)
	{
		index = reader.ReadInt32();
		name = reader.ReadString(reader.ReadUInt16());
		abilityType = reader.ReadInt32();
		autoAbility = reader.ReadInt32();
		delay = reader.ReadInt32();
		levelRestriction = reader.ReadInt32();
	}

	void PetAbilityTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "PET_ABILITY.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const PetAbility& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto PetAbilityTable::Find(int32_t index) const -> const PetAbility*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto PetAbilityTable::Get() const -> const std::vector<PetAbility>&
	{
		return _vector;
	}
}
