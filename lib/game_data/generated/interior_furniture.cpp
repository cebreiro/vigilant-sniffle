/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "interior_furniture.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<InteriorFurnitureTable> registry;

	InteriorFurniture::InteriorFurniture(StreamReader& reader)
	{
		index = reader.ReadInt32();
		furnitureId = reader.ReadInt32();
		furnitureType = reader.ReadInt32();
		furnitureLv = reader.ReadInt32();
		furnitureCost = reader.ReadInt32();
		furnitureDesc = reader.ReadInt32();
		furniturePreview = reader.ReadInt32();
		furnitureAnitype = reader.ReadInt32();
	}

	void InteriorFurnitureTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "INTERIOR_FURNITURE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const InteriorFurniture& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto InteriorFurnitureTable::Find(int32_t index) const -> const InteriorFurniture*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto InteriorFurnitureTable::Get() const -> const std::vector<InteriorFurniture>&
	{
		return _vector;
	}
}
