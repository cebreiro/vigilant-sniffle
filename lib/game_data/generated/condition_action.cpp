/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "condition_action.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ConditionActionTable> registry;

	ConditionAction::ConditionAction(StreamReader& reader)
	{
		index = reader.ReadInt32();
		groupId = reader.ReadInt32();
		partId = reader.ReadInt32();
		position = reader.ReadInt32();
	}

	void ConditionActionTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "CONDITION_ACTION.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const ConditionAction& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ConditionActionTable::Find(int32_t index) const -> const ConditionAction*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ConditionActionTable::Get() const -> const std::vector<ConditionAction>&
	{
		return _vector;
	}
}
