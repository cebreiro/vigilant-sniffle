/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "zone.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<ZoneTable> registry;

	Zone::Zone(StreamReader& reader)
	{
		index = reader.ReadInt32();
		zoneFilename = reader.ReadString(reader.ReadUInt16());
		zoneType = reader.ReadInt32();
		zoneDesc = reader.ReadString(reader.ReadUInt16());
		zoneBgm = reader.ReadInt32();
		zoneWeatherDbid = reader.ReadInt32();
		zoneAvatarLight = reader.ReadInt32();
		zoneAmbient = reader.ReadInt32();
		zoneEntryLv = reader.ReadInt32();
		zoneUseability = reader.ReadInt32();
		zoneUsereturn = reader.ReadInt32();
	}

	void ZoneTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "ZONE.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const Zone& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto ZoneTable::Find(int32_t index) const -> const Zone*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto ZoneTable::Get() const -> const std::vector<Zone>&
	{
		return _vector;
	}
}
