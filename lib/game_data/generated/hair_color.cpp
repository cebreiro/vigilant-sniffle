/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "hair_color.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<HairColorTable> registry;

	HairColor::HairColor(StreamReader& reader)
	{
		index = reader.ReadInt32();
		pnxValue = reader.ReadInt32();
		r = reader.ReadInt32();
		g = reader.ReadInt32();
		b = reader.ReadInt32();
	}

	void HairColorTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "HAIR_COLOR.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const HairColor& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto HairColorTable::Find(int32_t index) const -> const HairColor*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto HairColorTable::Get() const -> const std::vector<HairColor>&
	{
		return _vector;
	}
}
