/**********************************************************
         AUTO GENERATED FILE BY sox_code_generator
                   DO NOT EDIT THIS FILE
**********************************************************/
#include "cafefood.h"

#include "lib/common/stream/stream_reader.h"
#include "lib/game_base/data/sox/sox_file.h"
#include "lib/game_data/game_data_registry.h"

namespace cebreiro::gamedata
{
	[[maybe_unused]]
	static GameDataRegistry<CafefoodTable> registry;

	Cafefood::Cafefood(StreamReader& reader)
	{
		index = reader.ReadInt32();
		foodName = reader.ReadString(reader.ReadUInt16());
		foodModelid = reader.ReadInt32();
		foodEatType = reader.ReadInt32();
		foodEatCount = reader.ReadInt32();
		foodCount = reader.ReadInt32();
		foodPrice = reader.ReadInt32();
		foodDesc = reader.ReadString(reader.ReadUInt16());
		foodEffect = reader.ReadInt32();
		foodEffectValue = reader.ReadInt32();
	}

	void CafefoodTable::LoadFromFile(const std::filesystem::path& path)
	{
		gamebase::SoxFile fileData(path / "CAFEFOOD.sox");
		StreamReader reader(fileData.gameData.data(), fileData.gameData.size());

		_vector.reserve(fileData.rowCount);
		for (int32_t i = 0; i < fileData.rowCount; ++i)
		{
			_vector.emplace_back(reader);
		}

		for (const Cafefood& data : _vector)
		{
			_umap[data.index] = &data;
		}
	}

	auto CafefoodTable::Find(int32_t index) const -> const Cafefood*
	{
		auto iter = _umap.find(index);
		return iter != _umap.end() ? iter->second : nullptr;
	}

	auto CafefoodTable::Get() const -> const std::vector<Cafefood>&
	{
		return _vector;
	}
}
